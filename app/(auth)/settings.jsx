import { StyleSheet, Text, View } from 'react-native'

const Settings = () => {
    return (
        <View>
            <Text>Menü</Text>
        </View>
    );
};

export default Settings;

const styles = StyleSheet.create({})