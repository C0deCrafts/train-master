import { StyleSheet, Text, View } from 'react-native'

const Training = () => {
    return (
        <View>
            <Text>Groups</Text>
        </View>
    );
};

export default Training;

const styles = StyleSheet.create({})